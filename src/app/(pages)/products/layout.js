export const metadata = {
  title: "Products | Social Hardware",
  description: "Our Specialised and advanced Products.",
};

export default function RroductLayout({ children }) {
  return (
    <div>
      <div>{children}</div>
    </div>
  );
}
